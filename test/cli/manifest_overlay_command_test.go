package cli_test

// TestManifestOverlayRoutesToExpectedTargets was removed
// because it depended on specific product commands (aiapp, aitable, teambition)
// that are no longer available as handwritten commands after the
// protocol-first MCP refactoring. These products are now discovered
// dynamically from MCP servers, and their availability depends on the
// test environment's fixture data.
